# Proyecto de Reconocimiento Facial con CNN

Este proyecto utiliza Redes Neuronales Convolucionales (CNN) y OpenCV para detectar y reconocer rostros en tiempo real.

## ⚠️ Instalación del Modelo (Importante)
Debido a las políticas de tamaño de GitHub, el archivo del modelo entrenado no está incluido en este repositorio.

1. **Descarga el modelo** desde este enlace: [PEGAR AQUÍ TU LINK DE DRIVE]
2. Descomprime o coloca el archivo `modelo_facial_resnet_turbo.h5` en la carpeta principal del proyecto (junto a `CNNFamososReconocedor.py`).

## 🚀 Cómo ejecutar
1. Crea un entorno virtual:
   ```bash
   python -m venv .venv
